// Target (o player)
target = obj_player; // troque se seu player tiver outro nome

// Screen shake
shake_timer = 0;
shake_strength = 0;

// View size
view_w = camera_get_view_width(view_camera[0]);
view_h = camera_get_view_height(view_camera[0]);
