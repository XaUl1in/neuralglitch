// Velocidade da bala
speed = 12;
direction = point_direction(x, y, mouse_x, mouse_y);

speed = 12;
direction = point_direction(x, y, mouse_x, mouse_y);
image_angle = direction; // Faz o sprite apontar na direção do movimento